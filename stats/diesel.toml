# For documentation on how to configure this file,
# see diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "src/schema.rs"
filter = { except_tables = [
  "schema_migrations", "collation_test", "game_goals_versions", "roles_users", "ar_internal_metadata", "delayed_jobs"
]}
