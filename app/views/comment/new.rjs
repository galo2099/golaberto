if @comment.valid?
  page.insert_html :bottom, "comments", :partial => "comment/comment", :locals => { :comment => @comment }
  page.visual_effect :highlight, "comments", :duration => 3
else
  p @comment.errors
  page.alert(@comment.errors.full_messages)
end
